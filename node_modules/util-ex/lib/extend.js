(function() {
  var isArray, isFunction;

  isArray = require('./is/type/array');

  isFunction = require('./is/type/function');

  module.exports = function(dest, sources, filter) {
    var key, keys, src, value, _i, _j, _k, _l, _len, _len1, _len2, _len3;
    if (!isArray(sources)) {
      sources = [sources];
    }
    if (isFunction(filter)) {
      for (_i = 0, _len = sources.length; _i < _len; _i++) {
        src = sources[_i];
        if (!(src && src instanceof Object)) {
          continue;
        }
        keys = Object.keys(src);
        for (_j = 0, _len1 = keys.length; _j < _len1; _j++) {
          key = keys[_j];
          value = src[key];
          if (filter(key, value)) {
            dest[key] = value;
          }
        }
      }
    } else {
      for (_k = 0, _len2 = sources.length; _k < _len2; _k++) {
        src = sources[_k];
        if (!(src && src instanceof Object)) {
          continue;
        }
        keys = Object.keys(src);
        for (_l = 0, _len3 = keys.length; _l < _len3; _l++) {
          key = keys[_l];
          dest[key] = src[key];
        }
      }
    }
    return dest;
  };

}).call(this);

//# sourceMappingURL=extend.js.map
